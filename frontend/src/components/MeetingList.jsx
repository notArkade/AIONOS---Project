export default function MeetingList({ items = [] }) {
  return <section className="panel schedule-panel"><div className="section-heading"><div><p className="eyebrow">Calendar</p><h2>Upcoming schedule</h2></div><span className="text-link">{items.length} meetings</span></div>{items.length === 0 ? <p className="empty-state">No meetings are recorded.</p> : <div className="schedule-list">{items.slice(0, 5).map((item) => <article className="schedule-item" key={item.id}><time>{item.scheduled_start ? new Date(item.scheduled_start).toLocaleTimeString(undefined, { hour: "numeric", minute: "2-digit" }) : "Time unknown"}</time><div><h3>{item.title}</h3><p>{item.scheduled_start ? new Date(item.scheduled_start).toLocaleDateString(undefined, { weekday: "short", month: "short", day: "numeric" }) : "Date unknown"}</p><span className="tag">{item.status}</span></div></article>)}</div>}</section>;
}